[package]
name = "rutor"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = "0.12.15"
tokio = { version = "1.44.1", features = ["macros", "fs", "rt", "rt-multi-thread"] }

[profile.release]
strip = true	# strip symbols
opt-level = "z" # optimize for size
